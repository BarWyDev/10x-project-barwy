---
/**
 * Main Application Page
 * 
 * Full flashcard generation flow with real Supabase database:
 * 1. Select or create a deck
 * 2. Generate flashcards from text using AI
 * 3. Review and edit proposals
 * 4. Save to Supabase database
 * 
 * This is the PRODUCTION-READY version (no demo mode, no auth for now)
 */
import Layout from "../../layouts/Layout.astro";
import { FlashcardApp } from '../../components/FlashcardApp';
---

<Layout>
  <div class="min-h-screen bg-gradient-to-br from-blue-50 via-white to-purple-50">
    <div class="container mx-auto py-8 px-4">
      <!-- Header -->
      <div class="text-center mb-8">
        <h1 class="text-4xl font-bold text-gray-900 mb-2">
          Generator Fiszek z AI
        </h1>
        <p class="text-lg text-gray-600">
          Tworz fiszki edukacyjne z dowolnego tekstu przy użyciu sztucznej inteligencji
        </p>
        <div class="mt-4 inline-flex items-center px-4 py-2 rounded-full text-sm font-medium bg-green-100 text-green-800">
          ✅ Produkcja - Zapis do bazy Supabase
        </div>
      </div>

      <!-- Main App -->
      <FlashcardApp client:load />

      <!-- Footer Info -->
      <div class="mt-12 text-center text-sm text-gray-500">
        <p>Używasz testowego użytkownika (ID: 11111111-1111-1111-1111-111111111111)</p>
        <p className="mt-1">Wszystkie dane są zapisywane w bazie Supabase</p>
      </div>
    </div>
  </div>
</Layout>

<style>
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .container > * {
    animation: fadeIn 0.6s ease-out;
  }
</style>


