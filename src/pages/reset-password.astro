---
/**
 * Strona resetowania hasła
 * Layout: AuthLayout (bez nawigacji dla zalogowanych użytkowników)
 * Komponent: ResetPasswordForm
 *
 * W przyszłości: Strona będzie odbierać token resetowania hasła z URL
 * (parametr z Supabase auth flow)
 */
import AuthLayout from "../layouts/AuthLayout.astro";
import { ResetPasswordForm } from "../components/auth/ResetPasswordForm";

export const prerender = false;
---

<AuthLayout title="Resetuj hasło - Generator Fiszek">
  <ResetPasswordForm client:load />
</AuthLayout>
