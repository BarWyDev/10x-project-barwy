---
/**
 * AppLayout - Główny layout dla aplikacji
 * Używany dla wszystkich stron wymagających zalogowania
 * Zawiera nawigację, przycisk wylogowania i ochronę tras
 * Middleware sprawdzi czy użytkownik jest zalogowany
 */
import "../styles/global.css";
import { LogoutButton } from "../components/auth/LogoutButton";

interface Props {
  title?: string;
}

const { title = "Generator Fiszek - Aplikacja" } = Astro.props;

// Get authenticated user from middleware
const user = Astro.locals.user;

// Defense in depth - redirect if no user (middleware should handle this, but double check)
if (!user) {
  return Astro.redirect('/login');
}
---

<!doctype html>
<html lang="pl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/png" href="/favicon.png" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <!-- Header z nawigacją -->
    <header class="bg-white border-b border-gray-200 sticky top-0 z-50">
      <div class="container mx-auto px-4 py-4">
        <div class="flex items-center justify-between">
          <!-- Logo / Brand -->
          <div class="flex items-center gap-3">
            <a href="/app" class="text-2xl font-bold text-gray-900 hover:text-gray-700 transition-colors">
              🎴 Generator Fiszek
            </a>
          </div>

          <!-- Nawigacja -->
          <nav class="hidden md:flex items-center gap-6">
            <a href="/app" class="text-gray-700 hover:text-gray-900 font-medium transition-colors">
              Moje Fiszki
            </a>
            <a href="/app/decks" class="text-gray-700 hover:text-gray-900 font-medium transition-colors">
              Talie
            </a>
          </nav>

          <!-- User Actions -->
          <div class="flex items-center gap-4">
            <LogoutButton client:load />
          </div>
        </div>
      </div>
    </header>

    <!-- Main Content -->
    <main>
      <slot />
    </main>

    <!-- Footer -->
    <footer class="bg-white border-t border-gray-200 mt-auto">
      <div class="container mx-auto px-4 py-6 text-center text-sm text-gray-600">
        <p>Generator Fiszek z AI &copy; 2025</p>
      </div>
    </footer>
  </body>
</html>

<style>
  html,
  body {
    margin: 0;
    width: 100%;
    min-height: 100%;
    display: flex;
    flex-direction: column;
  }

  body {
    display: flex;
    flex-direction: column;
  }

  main {
    flex: 1;
  }
</style>


